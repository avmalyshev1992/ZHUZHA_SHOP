<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background = "@drawable/wallpaper4"
    tools:context=".MainActivity">


    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottom_navigation"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/teal"
        app:itemTextColor="@android:color/white"
        app:itemIconTint="@android:color/white"
        app:labelVisibilityMode="labeled"
        app:layout_constraintBottom_toBottomOf="parent"
        app:menu="@menu/menu"/>



    <ImageButton
        android:id="@+id/catImageButton"
        android:layout_width="320dp"
        android:layout_height="120dp"
        android:background="@drawable/custom_button1"
        android:layout_marginTop="208dp"
        android:elevation="20dp"
        android:scaleType="fitXY"
        android:src="@drawable/for_cats"
        app:layout_constraintBottom_toTopOf="@+id/dogImageButton"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintVertical_bias="0.123"
        tools:ignore="MissingConstraints" />

    <ImageButton
        android:id="@+id/dogImageButton"
        android:background="@drawable/custom_button1"
        android:layout_width="320dp"
        android:layout_height="120dp"
        android:layout_marginBottom="164dp"
        android:elevation="20dp"
        android:scaleType="fitXY"
        android:src="@drawable/for_dogs"
        app:layout_constraintBottom_toTopOf="@+id/bottom_navigation"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        tools:ignore="MissingConstraints" />


</androidx.constraintlayout.widget.ConstraintLayout>